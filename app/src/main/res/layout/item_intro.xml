<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <TextView
        android:id="@+id/txt_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:textColor="@color/black"
        android:textSize="42sp"
        android:fontFamily="@font/medel"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Temukan Tempat Wisata Baru" />

    <TextView
        android:id="@+id/txt_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/poppins"
        android:gravity="center"
        android:textColor="@color/gray_52"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/txt_title"
        tools:text="memandu Anda untuk mengeksplorasi tempat-tempat wisata baru dan unik" />

    <ImageView
        android:id="@+id/img_intro"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:scaleType="fitCenter"
        app:layout_constraintDimensionRatio="5:4"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txt_desc"
        tools:src="@drawable/intro1" />

</androidx.constraintlayout.widget.ConstraintLayout>